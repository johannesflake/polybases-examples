name: test

on:
#  push: #
  workflow_dispatch: # (manually)

jobs:
  test:
    runs-on: ubuntu-latest
      
    steps:
      - name: checkout
        uses: actions/checkout@v2
      
      - name: test
        uses: addnab/docker-run-action@v3
        with:
          image: johannesflake/oscar
          options: -v ${{ github.workspace }}:/home/oscar/work
          run: |
            echo Hello, world!
            julia -v
            cd work
            pwd
            ls
            julia tests.jl

